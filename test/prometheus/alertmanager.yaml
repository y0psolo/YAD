schemaVersion: "2.0.0"
commandTests:
  - name: alertmanager_version
    command: "/usr/bin/alertmanager"
    args: ["--version"]
    expectedOutput: ['alertmanager, version [0-9]+.[0-9]+.[0-9]+ \(branch: HEAD, revision: [a-z0-9]+\)']
    exitCode: 0